DF:				số comment chứa từ đấy.
TF:				số lần xuất hiện trong comment / số từ trong comment.
IDF:			log10(1 + num_of_comment / DF )


DF = 15
frequency = 1.


TF = 1
IDF = 


thật tuyệt_vời vì chủ_quyền đất_nước tôi sẵn_sàng đóng_góp và hy_sinh xương_máu để bảo_vệ 

select id, count(*) as sum0, cmt_segment, label from TblCommentTest where cmt_segment like N'% tuyệt_vời %' and id < 200 order by id
select id, count(*) as sum1, cmt_segment, label from TblCommentTest where cmt_segment like N'tuyệt_vời %' and id < 200 order by id 
select id, count(*) as sum2, cmt_segment, label from TblCommentTest where cmt_segment like N'% tuyệt_vời' and id < 200 order by id